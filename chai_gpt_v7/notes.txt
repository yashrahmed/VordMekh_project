Workflow -

Try DSPY for prompt tuning for atleast one of the steps.

- User asks for recipe for a certain type of chai (single serving) and goes back and forth with the customization.
- Using the recipe, the LLM is prompted to extract ingredients, their quantities and actions like crushing, grating, stirring, aerating etc.
- Lookup the chai prep tools frame table using the above step's output as the input.
- Combine the results from the previous steps with the cooking scenario descriptions like in V6.
- Have the LLM generate a tool/equipment description.


Tools and Libraries -
1. Langchain
2. Langgraph.
3. DSPy.
4.....Something from prompt caching.......

@Todo
- Simplify the system by using a single frame with different detected attributes?

Anywhere

  - IF scene_type=anywhere AND cooking_vessel_has_handle=false THEN require clamps/tongs.
  - IF scene_type=anywhere AND cooking_vessel_has_handle=false THEN require mittens.
  - IF scene_type=anywhere AND preparing_for_large_group=true THEN require stockpot with spigot.

  Home

  - IF scene_type=home AND heat_source=gas cooktop THEN require pot.
  - IF scene_type=home AND heat_source=induction cooktop THEN require induction pot.

  Campsite

  - IF scene_type=campsite AND windy=true THEN require windshield.
  - IF scene_type=campsite AND rainy=true THEN require rainfly.
  - IF scene_type=campsite AND rainy=true THEN require canopy.
  - IF scene_type=campsite AND dark=true THEN require lantern.
  - IF scene_type=campsite AND dark=true THEN require headlamp.
  - IF scene_type=campsite AND heat_source=butane stove THEN require butane canister.
  - IF scene_type=campsite AND heat_source=propane stove AND fuel=propane tank THEN require propane hose adapter.
  - IF scene_type=campsite AND heat_source=propane stove THEN require propane canister.
  - IF scene_type=campsite AND heat_source=induction stove THEN require induction pot AND battery.
  - IF scene_type=campsite AND ignition_needed=true THEN use lighter.
  - IF scene_type=campsite AND ignition_needed=true THEN use matches.
  - IF scene_type=campsite THEN require portable table.
